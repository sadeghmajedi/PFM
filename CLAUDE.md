# PFM — Personal Finance Manager

یک PWA تک‌فایلی برای مدیریت مالی شخصی، بدون build step و بدون backend. همه‌ی داده‌ها فقط در `localStorage` مرورگر/دستگاه کاربر ذخیره می‌شوند.

## معماری single-file

تقریباً کل منطق اپ (UI، استایل، JS) داخل **`index.html`** است (~4100 خط) — یک `<script>` واحد در انتهای فایل، بدون هیچ bundler یا framework. بخش‌ها با کامنت‌های جداکننده مشخص شده‌اند، مثلاً:

```
// ═══════════════════════════════════════
// DATABASE
// ═══════════════════════════════════════
```

بخش‌های اصلی به ترتیب در فایل: تقویم جلالی → `DATABASE` (`saveDB`/`loadDB`, کلید `pfm_db_v3`) → آپلود لوگو → PIN/قفل → ناوبری → مودال‌ها → توابع ثبت تراکنش → وام‌ها → گزارش‌ها → و در انتها **UPDATE CHECK / SERVICE WORKER**.

فایل‌های دیگر همگی جانبی و کوچک‌اند:
- `sw.js` — Service Worker (کش آفلاین)
- `manifest.json` — مانیفست PWA
- `version.json` — فقط برای تشخیص نسخه‌ی جدید توسط اپ (نه چیز دیگری)
- `icon-192.png` / `icon-512.png`

هیچ تست خودکار، لینتر یا مرحله‌ی build در ریپو وجود ندارد؛ تغییرات مستقیماً روی `index.html`/`sw.js` اعمال و منتشر می‌شوند (GitHub Pages).

## سه نقطه‌ی نسخه — همیشه هرسه با هم عوض شوند

نسخه در سه جای مستقل نگه‌داری می‌شود و **باید همیشه هم‌زمان با هم آپدیت شوند**، وگرنه منطق «بررسی به‌روزرسانی» نادرست کار می‌کند یا کش قدیمی می‌ماند:

1. **`version.json`** → کلید `"version"` — تنها منبعی که اپ از راه شبکه fetch می‌کند تا بفهمد نسخه‌ی جدیدتری منتشر شده یا نه.
2. **`sw.js`** → متغیر `CACHE_NAME` (مثلاً `pfm-cache-v1.9.4`) — تغییر این مقدار باعث می‌شود Service Worker کش قدیمی را دور بریزد و نسخه‌های تازه‌ی فایل‌ها را کش کند.
3. **`index.html`** → متغیر `CURRENT_VERSION` در بخش `UPDATE CHECK` (نزدیک انتهای `<script>`) — با این مقایسه می‌شود که آیا نسخه‌ی سرور از نسخه‌ی درحال‌اجرا جدیدتر است. `versionLabel` در تنظیمات از همین متغیر پر می‌شود (نباید مقدار ثابت hardcode شود).

## نکته‌ی حیاتی: iOS Safari دو فضای جدا برای Home Screen و تب Safari دارد

وقتی کاربر PFM را با «Add to Home Screen» روی iOS نصب می‌کند، آیکون روی هوم‌اسکرین یک **کانتکست وب کاملاً مجزا** از تب Safari معمولی است:

- **Service Worker جدا**: SW ثبت‌شده وقتی از تب Safari باز شده، با SW ثبت‌شده از آیکون هوم به‌عنوان یک origin/scope مشترک از نظر مشخصات مدیریت می‌شود ولی چرخه‌ی به‌روزرسانی و رفتار کش HTTP این دو context روی iOS مستقل و غیرقابل‌اعتماد است — SW نصب‌شده در حالت standalone (هوم‌اسکرین) اغلب هیچ‌وقت خودش را چک/آپدیت نمی‌کند مگر اینکه کاربر همان صفحه را در خود Safari هم باز کند.
- **`localStorage` جدا**: داده‌های ذخیره‌شده وقتی اپ از Safari باز شده، با داده‌های همان اپ وقتی از آیکون هوم باز شده **یکی نیستند** (storage partitioning جدا برای standalone PWA در iOS). یعنی کاربری که هم از Safari و هم از آیکون هوم استفاده می‌کند، ممکن است دو مجموعه داده‌ی متفاوت (دو تا `pfm_db_v3` جدا) ببیند.
- نتیجه‌ی عملی: **force close کردن اپ روی iOS کمکی به آپدیت SW نمی‌کند** — فقط پردازه‌ی صفحه را می‌بندد، مرورگر را مجبور به fetch دوباره‌ی `sw.js` از شبکه نمی‌کند. تنها راه‌های قابل‌اتکا: (الف) ثبت SW با `updateViaCache:'none'` (قانون بعدی)، (ب) باز کردن صفحه در خود Safari تا SW آن context هم آپدیت شود، یا (ج) پاک کردن داده‌های سایت از تنظیمات iOS.
- هر تغییری که رفتار storage یا SW را لمس می‌کند باید در نظر بگیرد که رفتار در این دو context می‌تواند متفاوت باشد؛ فرض نکنید یک بار تست در Safari یعنی همان چیز در حالت نصب‌شده هم درست کار می‌کند.

## قانون: ثبت Service Worker همیشه با `updateViaCache: 'none'`

```js
navigator.serviceWorker.register('./sw.js', { updateViaCache: 'none' });
```

بدون این گزینه، مرورگر (به‌خصوص iOS Safari) ممکن است خود فایل `sw.js` را مثل یک فایل استاتیک معمولی کش کند. در این حالت مرورگر حتی هرگز متوجه نمی‌شود که `sw.js` عوض شده، چون بایت‌های جدید را از شبکه نمی‌گیرد — و SW قدیمی (با `CACHE_NAME` و منطق قدیمی) برای همیشه فعال می‌ماند، حتی بعد از publish شدن نسخه‌ی جدید روی سرور. این دقیقاً همان باگی بود که در commit `b13e7ea` رفع شد؛ **هرگز این گزینه را از فراخوانی `register()` حذف نکنید.**

نکات مرتبط که باید حفظ شوند:
- fetch مربوط به `version.json` — چه در `index.html` و چه داخل `sw.js` — باید با `cache: 'no-store'` + یک پارامتر cache-busting انجام شود تا هیچ‌وقت از کش (نه کش مرورگر، نه Cache Storage) پاسخ کهنه برنگردد.
- بعد از فعال‌شدن SW جدید (`controllerchange`)، صفحه باید یک‌بار خودکار reload شود تا `index.html` تازه فوراً بارگذاری شود.

## داده‌ی کاربر

- کلید اصلی دیتابیس: `localStorage['pfm_db_v3']` (از طریق `saveDB()`/`loadDB()`).
- کلیدهای دیگر: `pfm_pin`, `pfm_last_backup_remind`, `pfm_version`.
- هیچ تغییری در منطق SW/آپدیت/کش نباید این کلیدها را پاک کند یا شکل داده را تغییر دهد؛ منطق آپدیت و منطق داده باید کاملاً مستقل بمانند.
